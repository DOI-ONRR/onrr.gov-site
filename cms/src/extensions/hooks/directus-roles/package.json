{
	"name": "directus-roles",
	"description": "Hook extension for creating and updating Directus roles in upstream CMS instances",
	"icon": "extension",
	"version": "1.0.0",
	"keywords": [
		"directus",
		"directus-extension",
		"directus-extension-hook"
	],
	"type": "module",
	"files": [
		"dist"
	],
	"directus:extension": {
		"type": "hook",
		"path": "dist/index.js",
		"source": "src/index.js",
		"host": "^12.0.0"
	},
	"scripts": {
		"build": "directus-extension build; mkdir -p ../../../../extensions/directus-extension-directus-roles/dist; cp package.json ../../../../extensions/directus-extension-directus-roles; cp dist/index.js ../../../../extensions/directus-extension-directus-roles/dist",
		"dev": "directus-extension build -w --no-minify",
		"link": "directus-extension link",
		"validate": "directus-extension validate"
	},
	"devDependencies": {
		"@directus/extensions-sdk": "17.1.2"
	}
}
