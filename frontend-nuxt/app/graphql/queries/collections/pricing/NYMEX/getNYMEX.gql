query GetNYMEX($statuses: [String]) {
  NYMEX(sort: ["date"], limit: -1, filter: { status: { _in: $statuses } }) {
    id
    date
    average
    roll
    excel_file: Spreadsheet {
      filename_download
    }
  }
}
