query GetIndianGasMajorPortion($statuses: [String]) {
  indian_gas_major_portion(
    sort: ["-date"]
    limit: -1
    filter: { status: { _in: $statuses } }
  ) {
    id
    date
    index_zones
  }
}
