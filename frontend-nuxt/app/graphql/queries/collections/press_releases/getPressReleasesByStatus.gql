query GetPressReleasesByStatus($statuses: [String]) {
  press_releases(
    limit: -1
    sort: "-date"
    filter: { status: { _in: $statuses } }
  ) {
    id
    status
    title
    date
    file {
      id
      filename_download
      title
    }
    link
    excerpt
    status
  }
}
