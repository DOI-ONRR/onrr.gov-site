name: Route Service Pipeline

on:
  push:
    paths: 'route-service/**'
    branches-ignore:
      - main
      - dev
  
  pull_request:
    paths: 'route-service/**'
    types: [closed]
    branches:
      - dev
      - main

  workflow_dispatch:

jobs:
  deploy-route-service:
    if: |
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'push' || 
      (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    runs-on: ubuntu-latest
    # Prod cloud.gov credentials live only in the `prod` environment, which is
    # restricted to the main branch; every other branch gets `nonprod`.
    environment: ${{ github.ref_name == 'main' && 'prod' || 'nonprod' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Deploy frontend
        working-directory: route-service
        run: ../.github/scripts/deploy.sh --branch "$BRANCH"
        env:
          BRANCH: ${{ github.ref_name }}
          CF_USERNAME: ${{ secrets.CF_USERNAME }}
          CF_PASSWORD: ${{ secrets.CF_PASSWORD }}